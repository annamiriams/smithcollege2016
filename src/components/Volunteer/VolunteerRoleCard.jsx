const VolunteerRoleCard = ({ title, description }) => {
    return (
        <div className="role-card">
            <h4>{title}</h4>
            <p>{description}</p>
        </div>
    );
};


export default VolunteerRoleCard;